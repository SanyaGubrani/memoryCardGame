import { GameLevel } from "@/types";

export const GAME_LEVELS: GameLevel[] = [
    { name: "Easy", numberOfCards: 5, path: "easy" },
    { name: "Medium", numberOfCards: 7, path: "medium" },
    { name: "Hard", numberOfCards: 10, path: "hard" },
];
